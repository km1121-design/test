services:
  - type: web
    name: invoice-app
    runtime: node
    plan: free
    rootDir: invoice-app/server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
